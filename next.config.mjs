/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "external-content.duckduckgo.com",
      "cdn.hobbyconsolas.com",
      "uunxjeernqlrui5u.public.blob.vercel-storage.com",
    ],
  },
};

export default nextConfig;
